import styled from 'styled-components';

export const Title = styled.h1`
  font-size: ${(props) => props.theme.text.FONT_SIZE_XXLARGE};
  color: ${(props) => props.theme.colors.GREY_ULTRA_DARK};
  font-weight: 300;
`;
